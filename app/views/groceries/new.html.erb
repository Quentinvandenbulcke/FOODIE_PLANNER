  <% require 'bigdecimal'
  %>

<div class="container">
  <div class="grocery-dates">
    <%= simple_form_for(@grocery) do |f| %>
      <%= f.association :meal_days, as: :check_boxes, label_method: ->(meal_day) { "#{meal_day.date} - #{meal_day.recipe.name} (#{meal_day.quantity}pers.)"} %>
      <%= f.submit %>
    <% end %>
  </div>

</div>
